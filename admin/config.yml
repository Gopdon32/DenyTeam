backend:
  name: github
  repo: "Gopdon32/DenyTeam"
  branch: "main"
  auth_type: "oauth"
  # Disable local backend so CMS uses GitHub OAuth to commit files
  local_backend: false
  # Explicit Netlify site slug for auth proxy
  site_id: "charming-sherbet-9d1521"
  # To enable login, register a GitHub OAuth App and set the callback URL below
  # 1) Register OAuth App in GitHub (Settings → Developer settings → OAuth Apps)
  # 2) Set Authorization callback URL to: https://charming-sherbet-9d1521.netlify.app/admin/
  # 3) Copy Client ID and paste it below
  client_id: "Ov23liCffDpzNbJ9SL19"

media_folder: "assets/uploads"
public_folder: "/assets/uploads"

collections:
  - name: "posts"
    label: "Posts"
    folder: "content/posts"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Date", name: "date", widget: "datetime" }
      - { label: "Tags", name: "tags", widget: "list" }
      - { label: "Body", name: "body", widget: "markdown" }

  - name: "projects_json"
    label: "Projects Index"
    files:
      - label: "Projects JSON"
        name: "projects"
        file: "data/projects.json"
        fields:
          - { label: "Raw JSON", name: "content", widget: "text" }
